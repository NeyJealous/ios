# Scoped tooling instructions

Наследовать корневые правила без ослабления. Governance tools должны быть
cross-platform, детерминированными, fail-closed, без секретов, production API и
remote writes. Ошибки валидации должны завершаться ненулевым exit code.
