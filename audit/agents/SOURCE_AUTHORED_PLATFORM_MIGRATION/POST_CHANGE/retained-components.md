# Retained components

- five source-authored canonical TOML profiles;
- capability contracts and deny-by-default validation;
- Registry, Review Matrix and Resolver fail-closed governance;
- immutable upstream snapshots, licenses, upstream lock and selection register;
- trusted execution attestation, now bound to profile and capability hashes;
- privacy/secret scan and governance test suite;
- activation policy as a validation-only owner gate.
