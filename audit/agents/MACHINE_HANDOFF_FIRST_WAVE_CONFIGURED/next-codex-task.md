# Next Codex task

From a fresh clone and a fresh Codex session:

1. verify canonical SHA, tag, clean status and exact five profile hashes;
2. run `npm ci`, `npm run agents:bootstrap`, `npm run agents:check`;
3. inspect the runtime-supported agent catalog without simulation;
4. probe each exact first-wave Agent ID;
5. if any type is unknown, preserve exact client/version evidence and stop;
6. if all are discovered, request a new owner gate before real runtime smoke.

Do not diagnose or repair production, Apps Script, Sheets, broker/API,
deployment, Market Regime, R030, Decision Engine or TradePlan state.
