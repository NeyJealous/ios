# Model runtime smoke acceptance report

Status: `BLOCKED_EXTERNAL_ATTESTATION_MISSING`

- Terra: `RUNTIME_AVAILABLE`.
- Luna: `MODEL_NOT_AVAILABLE` after exact provider rejection.
- Sol: `RUNTIME_AVAILABLE`.
- Sol Ultra: `RUNTIME_AVAILABLE` as `gpt-5.6-sol` with `ultra` reasoning.
- Substitution: `false`.
- Platform activation eligibility: `false` for every model.

This versioned gate supersedes earlier Sol Pro statements only for current model routing. Historical Phase 3A reports and manifests remain unchanged and bound to their original SHA.
